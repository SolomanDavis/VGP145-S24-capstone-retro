using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeavyEnemy : Enemy

    // private int _lives
{
    // Start is called before the first frame update
    void Start()
    {
        

    }

    // Update is called once per frame
    void Update()
    {

        /* bool isFacingDown()
        {
        using raccasting
        if true shoot()
        use player location
        } 
        */

        /* onCollision with playerProjectile()
        {
           call damageFunction()
        } */


    }

    //  void heaveyEnemyDamage()
    //{}

    // void changeColor()
    //{  lives -1 }

    // void heaveyDeath()
    // call EnemyDeath () when lives == 0
}

